# GEMINI.md

This project's agent instructions live in **[AGENTS.md](./AGENTS.md)** — read that first.

Then, depending on the task, read the specific file under `docs/` that `AGENTS.md` §3 points you to. Don't guess at endpoints, schema fields, or roles that aren't written down in `docs/` — the docs are the ground truth, not general knowledge about "typical" healthcare apps.

If you're about to create a new file, check `docs/06-folder-structure.md` first so it lands in the right place.
